# =============================================================================
# GASdf Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values

# =============================================================================
# Environment
# =============================================================================
# Options: development, staging, production
NODE_ENV=development

# Server port
PORT=3000

# Allowed CORS origins (comma-separated, production only)
# Example: https://myapp.com,https://api.myapp.com
ALLOWED_ORIGINS=

# =============================================================================
# Solana RPC
# =============================================================================
# Helius API key for enhanced RPC (recommended)
# Get one at: https://www.helius.dev/
HELIUS_API_KEY=your_helius_api_key

# Custom RPC URL (overrides HELIUS_API_KEY selection)
# If not set, uses Helius devnet/mainnet based on NODE_ENV
# RPC_URL=

# =============================================================================
# Redis
# =============================================================================
# REQUIRED in production (no in-memory fallback)
# Development: optional (falls back to in-memory store)
REDIS_URL=redis://localhost:6379

# =============================================================================
# Fee Payer Configuration
# =============================================================================
# REQUIRED: Base58-encoded private key for the fee payer wallet
# This wallet pays transaction fees on behalf of users
# SECURITY: Never commit this value! Use secrets management in production
FEE_PAYER_PRIVATE_KEY=your_base58_private_key

# Multi fee payer keys (Phase 2 - comma-separated base58 keys)
# Provides redundancy if one wallet runs low on SOL
# FEE_PAYER_KEYS=key1,key2,key3

# =============================================================================
# Token Configuration
# =============================================================================
# $ASDF token mint address
# Mainnet: 9zB5wRarXMj86MymwLumSKA1Dx35zPqqKfcZtK1Spump
# Devnet: uses placeholder (burn worker disabled)
ASDF_MINT=9zB5wRarXMj86MymwLumSKA1Dx35zPqqKfcZtK1Spump

# =============================================================================
# Fee Settings
# =============================================================================
# Base fee in lamports (default: 5000 = 0.000005 SOL)
BASE_FEE_LAMPORTS=5000

# Fee multiplier for profit margin (default: 1.5 = 50% markup)
FEE_MULTIPLIER=1.5

# Quote TTL in seconds (default: 60)
QUOTE_TTL_SECONDS=60

# =============================================================================
# Burn Worker Settings
# =============================================================================
# Minimum accumulated fees (in lamports) before triggering burn
# Default: 100000000 = 0.1 SOL
BURN_THRESHOLD_LAMPORTS=100000000

# =============================================================================
# Oracle (Optional)
# =============================================================================
# K-score oracle API URL
# If not set, uses trusted tokens list + STANDARD fallback
# ORACLE_URL=https://oracle.asdfasdfa.com

# Oracle API key (optional, for higher rate limits)
# ORACLE_API_KEY=

# =============================================================================
# Monitoring (Phase 3)
# =============================================================================
# Enable Prometheus metrics endpoint (/metrics)
# PROMETHEUS_ENABLED=true

# Webhook URL for alerts (Slack, Discord, PagerDuty, etc.)
# ALERTING_WEBHOOK=https://hooks.slack.com/services/...

# =============================================================================
# Environment-Specific Overrides
# =============================================================================
# Development:
#   - Redis optional (in-memory fallback)
#   - FEE_PAYER_PRIVATE_KEY optional (limited functionality)
#   - Uses devnet RPC
#
# Staging:
#   - Redis REQUIRED
#   - FEE_PAYER_PRIVATE_KEY REQUIRED
#   - Uses devnet RPC
#
# Production:
#   - Redis REQUIRED (strict)
#   - FEE_PAYER_PRIVATE_KEY REQUIRED
#   - ASDF_MINT REQUIRED (valid mainnet address)
#   - Uses mainnet RPC
